# buyerA 结账用的出站 IP：一行一个，第一行是当前用的，后面是备用（被 541 后 GET /rotate 换）。
# 只写确实挂在本机网卡、能出网的公网 IP（用 list-ips.py 验证）。
# 不能和 ips.txt（监控池）或其他账号的文件重叠。改好后存成 buyer-a.txt（去掉 .example）。
203.0.113.21
203.0.113.22
203.0.113.23
