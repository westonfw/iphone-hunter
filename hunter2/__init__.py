"""hunter2：把「盯」和「买」拆成两层，中间用局域网信号总线连起来。

为什么要拆（2026-09-20 的实测账）：

* 结账是 6 次串行往返、每次约 10 秒，加提交约 60 秒。**平峰时段测过，动不了。**
* 第 2 步的 signKey 不是库存占用，货必须撑到提交那一刻。
* 所以一个账号在一次放货里只有**一发子弹**，而需要的窗口是 65 秒以上。

盯和买的性质正好相反：

            盯                          买
  登录      不要（pickup 是公开接口）    要，且绑会话和出口 IP
  并行      能，按出口 IP 横向扩         不能，一个账号一次一发
  稀缺      出口 IP                      **Apple ID**

揉在一台机器里的代价，2026-09-20 08:56 那波看得很清楚：8 个型号同时放货，
买手串行地先打了一个已经卖完的型号，白烧 13 秒；轮到真正有货的那个时，
窗口只剩一半，最后在第 6 步差 2 秒被拒。

拆开之后：探针按 IP 扩（永不失明）、买手按账号扩（每个账号一发子弹）、
多型号同放时分工不撞车。**六步那 60 秒一秒也快不了**——这一层只负责让
子弹更多、瞄得更准、不脱靶。

实现上不 fork：`hunter.fastpath` / `hunter.checkout` / `hunter.autobuy` /
`hunter.purchase_guard` 原样复用，这里只放拓扑。
"""
