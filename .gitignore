config.json
state.json
scout-state.json
checkout-trace.jsonl
__pycache__/
*.pyc
.venv/
.browser-profile/
logs*/

# 密码和总线密钥只放环境变量；.env 是给启动脚本 source 用的，绝不能进仓库
.env

# HAR 里有 cookie / 身份证 / 手机号明文，绝不能进仓库
*.har

.purchase.lock
order-attempt.json
launch-state.json

# 代理的真实 IP 清单（监控池 + 每个账号一份），只提交 .example
tools/ips.txt
tools/buyer-*.txt
tools/proxy.env
