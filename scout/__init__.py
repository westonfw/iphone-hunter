"""主程序：一个进程轮转所有出口 IP 去刷库存，看到货就广播给买手。

跟买手分开成独立的包，是因为它是**另一种程序**：不要账号、不开浏览器、
没有订单状态。混在一个目录里只会让日志和状态互相纠缠。

结构是「一主多子」：

    主程序启动 →  轮转 [直连] + [各子程序的转发口] 刷库存
    子程序启动 →  广播「我在，我的转发口是 N 号」
               →  同时替主程序转发请求，等于把自己的出口 IP 借出去
    主程序     →  看到货 → 广播 → 各子程序自己去买

这么摆的好处是**每加一个买手，就同时多一个账号和一个出口 IP**——两种稀缺资源
一起涨，而且不用买代理。

为什么非要收进一个进程：N 个独立探针各跑各的，相位是随机的。2026-09-20 两台
的实测，A 中位 32.4s、B 中位 52.8s，合并只有 24.4s，而且 **7% 的巡检是两台
几乎同时打的，那一次等于白费**。收进一个进程之后，调度就退化成一个 for 循环，
相位协调这个问题根本不存在。

另一个更大的好处：某个出口被 541 拉黑时，跳过它用下一个，**系统零失明**。
今天本机 9%、另一台 20% 的失明时间直接归零。

主程序是单点，挂了所有买手一起瞎——按约定不做保险丝，挂了就告警。
"""
